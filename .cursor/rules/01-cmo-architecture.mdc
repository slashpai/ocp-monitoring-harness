---
description: CMO architecture - component catalog, repo mapping, and development context
globs: ARCHITECTURE.md
alwaysApply: false
---

# Cluster Monitoring Operator Architecture

For component relationships, upstream/downstream repo mapping, and syncbot workflow, see [`ARCHITECTURE.md`](../../ARCHITECTURE.md).

For CMO internals (reconciliation, config API, jsonnet pipeline, namespace topology, multi-module structure), see [`projects/cluster-monitoring-operator/AGENTS.md`](https://github.com/openshift/cluster-monitoring-operator/blob/main/AGENTS.md) — maintained alongside the code.
